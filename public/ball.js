class Ball {
    constructor(x, y) {
        this.color = (115, 115, 155);
        this.radius = 50;
        this.y = 0;
        this.x = 0;
        this.r = 50;
        this.xspeed = xspeed
        this.yspeed = this.yspeed
    }

    show() {
        ellipse(this.move(), this.y, this.radius)
    }
    move() {
        this.x += this.xspeed
        this.y += this.yspeed
    }
hitsWall(w, h ){
    if(this.hitwall){}
}
}